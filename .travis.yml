language: node_js
node_js:
  - 8
cache:
  directories: node_modules
script: npm run build
deploy:
  local_dir: /dist
  provider: pages
  skip_cleanup: true
  github_token: $ssh_key # Set in the settings page of your repository, as a secure variable
  keep_history: false
  on:
    branch: master
